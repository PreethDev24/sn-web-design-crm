# Historical schema reference

These SQL migrations were used when the CRM ran on Supabase Postgres.

The live schema is now provisioned with:

```bash
npm run appwrite:setup
```

See `scripts/appwrite/README.md` for export/import and cutover steps.
